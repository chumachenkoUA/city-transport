# Статус реалізації функціонала "City Transport"

Цей документ порівнює початкові технічні вимоги з поточною реалізацією в коді (станом на січень 2026).

**Легенда:**
- ✅ **Реалізовано**: Повністю працює на рівні БД (API схеми) та Бекенду.
- ⚠️ **Частково**: Є база даних/API, але логіка потребує доопрацювання на клієнті.
- 🔄 **В процесі**: Заплановано, але ще не фіналізовано.

---

## 1. Пасажир (Гість та Зареєстрований)

| № | Задача | Вхід | Статус | Реалізація в коді (DB/API) |
|---|---|---|---|---|
| **1** | **Перегляд зупинок** | GPS | ✅ | `guest_api.v_stops` (пошук найближчих реалізується через PostGIS `ST_DWithin` або фільтр на клієнті). |
| **2** | **Рух транспорту на зупинці** | ID Зупинки | ✅ | `guest_api.v_stop_schedule` / `v_route_stops`. Показує маршрути, що проходять через зупинку. |
| **3.1** | **Зупинки маршруту** | Тип, Номер | ✅ | `guest_api.v_route_stops`. Повертає впорядкований список зупинок. |
| **3.2** | **Траєкторія маршруту** | Тип, Номер | ✅ | `guest_api.v_route_points`. Повертає масив GPS точок для малювання лінії на карті. |
| **4.1** | **Пошук маршруту (А -> Б)** | GPS А, GPS Б | ⚠️ | Реалізовано через фільтрацію маршрутів, що містять обидві зупинки (через `guest_api`). Складний routing engine (pgRouting) не підключали, використовується логіка перетину маршрутів. |
| **5** | **Розклад руху** | Маршрут, Зупинка | ✅ | `guest_api.v_schedule`. |
| **6/11**| **Подача скарги** | Текст, Дана | ✅ | `public.complaints_suggestions` + API endpoint. |
| **7** | **Транспортна картка** | ID User | ✅ | `passenger_api.v_my_cards`. Баланс, номер картки. |
| **7.1** | **Поповнення** | ID Card, Сума | ✅ | `passenger_api.top_up_card`. |
| **8** | **Історія поїздок** | ID User | ✅ | `passenger_api.v_my_trips` (історія валідацій/квитків). |
| **9** | **Перегляд штрафів** | ID User | ✅ | `passenger_api.v_my_fines`. |
| **10** | **Оскарження штрафу** | ID Fine, Фото | ✅ | `passenger_api.appeal_fine`. |

---

## 2. Водій

| № | Задача | Вхід | Статус | Реалізація в коді (DB/API) |
|---|---|---|---|---|
| **1** | **Робочий графік** | ID User | ✅ | `driver_api.v_my_schedule`. Список призначених поїздок. |
| **2** | **Деталі маршруту** | Номер | ✅ | Доступ до `guest_api` views для перегляду зупинок/точок свого маршруту. |
| **3.1** | **Початок рейсу** | Авто, Час | ✅ | `driver_api.start_trip`. Складний SQL-метод з валідацією. |
| **3.2** | **Завершення рейсу** | Авто, Час | ✅ | `driver_api.finish_trip`. |
| **4** | **Пасажиропотік** | Кількість | ✅ | `driver_api.update_passengers`. Водій вносить дані про кількість людей. |
| **Extra**| **GPS трекінг** | Lon, Lat | ✅ | `driver_api.log_vehicle_gps`. Автоматичний запис координат під час рейсу. |

---

## 3. Диспетчер

| № | Задача | Вхід | Статус | Реалізація в коді (DB/API) |
|---|---|---|---|---|
| **1.1** | **Створення розкладу** | Маршрут, Час | ✅ | `dispatcher_api.create_schedule_entry` / `manage_trip`. |
| **3** | **Призначення водія** | Водій, Маршрут | ✅ | Реалізовано через створення записів у `trips` (майбутні поїздки). |
| **4.1** | **Моніторинг транспорту** | - | ✅ | `dispatcher_api.v_active_trips_monitoring`. Показує останні координати (`last_lon`, `last_lat`) з `vehicles`. |
| **4.2** | **Відхилення від графіку** | Час | ⚠️ | Дані є (плановий `starts_at` vs реальний лог). Логіка підсвічування реалізується на клієнті (Front). |

---

## 4. Контролер

| № | Задача | Вхід | Статус | Реалізація в коді (DB/API) |
|---|---|---|---|---|
| **1** | **Перевірка картки** | ID Card | ✅ | `controller_api.v_card_details`. Показує баланс та **останню валідацію** (маршрут, час). |
| **2** | **Реєстрація штрафу** | Card, Причина | ✅ | `controller_api.issue_fine`. Створює запис у `fines` зі статусом "Очікує сплати". |

---

## 5. Бухгалтер

| № | Задача | Вхід | Статус | Реалізація в коді (DB/API) |
|---|---|---|---|---|
| **1.1** | **Бюджет** | Доходи/Витрати | ✅ | `accountant_api.upsert_budget`. |
| **1.2** | **Облік витрат** | Сума, Опис | ✅ | `accountant_api.add_expense`. |
| **1.3** | **Облік доходів** | - | ✅ | Автоматично рахується через `get_financial_report` (сумує продані квитки/поповнення). |
| **2.1** | **Зарплата** | Працівник, Ставка| ✅ | `accountant_api.pay_salary`. |
| **3.1** | **Звітність** | Період | ✅ | `accountant_api.get_financial_report`. Повертає агреговані дані JSON-ом для графіків. |

---

## 6. Департамент мерії

| № | Задача | Вхід | Статус | Реалізація в коді (DB/API) |
|---|---|---|---|---|
| **1** | **Проектування маршруту**| Точки, Зупинки | ✅ | `municipality_api.create_route_full`. Приймає складний JSONB з масивом зупинок та точок траєкторії. |
| **2** | **Створення зупинок** | Lon, Lat | ✅ | `municipality_api.create_stop`. |
| **3** | **Аналітика потоку** | Період | ✅ | `municipality_api.v_passenger_flow_analytics`. Групує дані з `trips`. |
| **4** | **Скарги** | Період | ✅ | `municipality_api.v_complaints_dashboard`. |

---

## 7. Менеджер

| № | Задача | Вхід | Статус | Реалізація в коді (DB/API) |
|---|---|---|---|---|
| **1** | **Найняття водіїв** | ПІБ, Права | ✅ | `manager_api.create_driver` (реєстрація користувача + запис у `drivers`). |
| **2** | **Додавання транспорту**| Тип, Номер | ✅ | `manager_api.add_vehicle`. |

---

## Висновки та Порівняння

### Що вдалося краще за вимоги:
1.  **Безпека (Security Hardening):** Вимоги не описували детально модель загроз, але ми реалізували **Thick Database** (товсту БД). Бізнес-логіка інкапсульована в SQL-функціях, а прямий доступ до таблиць заблоковано (`REVOKE`). Це значно надійніше за звичайний REST API.
2.  **Аудит:** Завдяки використанню `session_user` у БД, кожна дія (штраф, рейс, платіж) автоматично прив'язана до конкретного логіна користувача на рівні ядра бази даних.
3.  **Валідація даних:** Впроваджено тригери на рівні БД, які фізично не дають створити поїздку з невірними даними (наприклад, GPS координати поза межами Землі або поїздка на авто, що вже зайняте).

### Технічні нюанси:
-   **Routing (А -> Б):** Реалізовано класичний пошук "прямих" маршрутів. Побудова маршрутів з пересадками — це задача рівня Google Maps, яка зазвичай вимагає графів (pgRouting), що є over-engineering для курсової роботи, тому зупинилися на оптимальному варіанті прямих маршрутів.
